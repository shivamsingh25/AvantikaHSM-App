import 'package:flutter/material.dart';

class Visitorentry extends StatelessWidget {

  @override
  Widget build(BuildContext context){
    return new Container(
      child: new Center(
        child: new Icon(Icons.assignment, size: 150.0, color: Colors.red)
      )
    );
  }
}